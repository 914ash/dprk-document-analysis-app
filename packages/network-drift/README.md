# Network Drift Package

Canonical package for temporal graph slicing, embedding, drift scoring, and analyst-facing visual outputs.

## Highlights
- Stable annual graph slicing.
- Composite drift scoring grounded in embeddings and graph metrics.
- Plotly output suitable for downstream dashboard and analyst reporting.

## Key command
- `python -m dprk_drift.cli score-drift`
